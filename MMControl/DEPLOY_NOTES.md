# MMControl Pages Notes

This folder is ready to serve as the project page source directory for a GitHub Pages site.

## Already cleaned

- Added a valid favicon at `static/images/favicon.svg`
- Replaced placeholder page title and Open Graph metadata
- Removed the broken `./supp/swiper-bundle.min.css` reference
- Replaced the anonymous author placeholder with a release reminder

## Still recommended before public release

- Replace the author list with final names and affiliations
- Add GitHub / paper / project links near the title block
- Re-enable and finalize the abstract section if needed
- Replace any remaining submission-era wording
- Do one cleanup pass on the HTML head structure to merge duplicate `<head>` blocks

## Deployment target

If this directory is placed at:

`<owner>.github.io/MMControl/`

then the final public URL will be:

`https://<owner>.github.io/MMControl/`
